<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="utn.aplicaciones.riquelmito">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TrabajosDeInteresActivity"></activity>
        <activity android:name=".MenuPostulanteTemporal" />
        <activity android:name=".PerfilUsuarioPostulanteActivity" />
        <activity android:name=".PerfilProfesionalActivity" />
        <activity android:name=".CrearTrabajoActivity" />
        <activity android:name=".BuscarTrabajoActivity" />
        <activity android:name=".CrearCuentaActivity" />
        <activity android:name=".TrabajosPostuladosActivity" />
        <activity
            android:name=".ConfiguracionNotificacionesPostulanteActivity"
            android:label="@string/title_activity_configuracion_notificaciones_postulante" />
        <activity
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>